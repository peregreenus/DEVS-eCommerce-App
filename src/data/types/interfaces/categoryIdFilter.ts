/* eslint-disable @typescript-eslint/naming-convention */
export default interface categoryIdFilter {
  categoryId: string;
  setCategoryId: React.Dispatch<React.SetStateAction<string>>;
  // eslint-disable-next-line semi
}
